# Application Configuration
APP_NAME=CC-Multi
# Observability Server Configuration
# For local development: http://localhost:4000
# For Docker containers: http://host.docker.internal:4000  
# Leave blank for automatic discovery (tries localhost, then Docker fallback)
OBSERVABILITY_SERVER_URL=http://localhost:4000

# Engineer Name (optional)
ENGINEER_NAME=Yosi